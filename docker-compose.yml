version: '3.8'

services:
  nginx:
    image: nginx:latest
    container_name: practica6_nginx
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
    restart: unless-stopped